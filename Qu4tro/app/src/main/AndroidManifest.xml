<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.octo.board">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED"></uses-permission> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.br.octo.board.MainActivity"
            android:configChanges="locale|layoutDirection"
            android:label="@string/app_name"
            android:resizeableActivity="true"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.br.octo.board.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.br.octo.board.DeviceListActivity"
            android:parentActivityName="com.br.octo.board.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.br.octo.board.SettingsActivity"
            android:configChanges="locale|layoutDirection"
            android:parentActivityName="com.br.octo.board.MainActivity" />
        <activity
            android:name="com.br.octo.board.LightSettingsActivity"
            android:parentActivityName="com.br.octo.board.MainActivity">
        </activity>

        <service
            android:name="com.br.octo.board.Tracking"
            android:enabled="true"
            android:exported="true"
            android:process=":tracking" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.br.octo.board.MapsActivity"
            android:label="@string/title_activity_maps">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.br.octo.board.MainActivity" />
        </activity>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="37f823ac90b1b11443514a9d32c0da7164d30520" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>